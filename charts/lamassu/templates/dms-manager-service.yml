apiVersion: v1
kind: Service
metadata:
  name: dms-manager
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: dms-manager
  type: ClusterIP
  ports:
  - name: https
    port: 8085
    targetPort: 8085
    protocol: TCP