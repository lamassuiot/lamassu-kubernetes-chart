apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dms-manager-cert
  namespace: {{.Release.Namespace}}
spec:
  isCA: false
  commonName: dms-manager*
  duration: {{.Values.tls.duration}}
  dnsNames:
    - "dms-manager"
    - "dms-manager*"
  secretName: dms-manager-upstream-cert
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: upstream-ca-issuer
    kind: Issuer
    group: cert-manager.io