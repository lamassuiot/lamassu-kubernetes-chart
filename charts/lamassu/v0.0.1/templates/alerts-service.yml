apiVersion: v1
kind: Service
metadata:
  name: alerts
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: alerts
  type: ClusterIP
  ports:
  - name: https
    port: 8085
    targetPort: 8085
    protocol: TCP