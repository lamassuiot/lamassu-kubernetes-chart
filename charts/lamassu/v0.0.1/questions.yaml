labels:
  io.cattle.role: project # options are cluster/project
categories:
- PKI
- IoT

questions:

- variable: storageClassName
  label: Kubernetes Storage Class Name
  description: ""
  group: "Kubernetes General Config"
  type: storageclass
  default:
  required: true

- variable: domain
  label: Domain
  description: Domain used to access the UI, APIs and other services exposed
  group: "Lamassu Config"
  type: string
  default: dev.lamassu.io
  required: true

- variable: consul.server.replicas
  label: Consul Replicas
  group: "Consul Config"
  type: int
  default: 3
  required: true

- variable: vault.server.ha.replicas
  label: Vault Replicas
  group: "Vault Config"
  type: int
  default: 3
  required: true