labels:
  io.cattle.role: project # options are cluster/project
categories:
- PKI
- IoT

questions:

- variable: storageClassName
  label: Kubernetes Storage Class Name
  description: ""
  group: "Kubernetes General Config"
  type: storageclass
  default: local-path
  required: true

- variable: domain
  label: Domain
  description: Domain used to access the UI, APIs and other services exposed
  group: "Lamassu Config"
  type: string
  default: dev.lamassu.io
  required: true

- variable: hashicorp/consul.server.replicas
  label: Consul Replicas
  group: "Consul Config"
  type: int
  default: 3
  required: true

- variable: hashicorp/vault.server.replicas
  label: Vault Replicas
  group: "Vault Config"
  type: int
  default: 5
  required: true