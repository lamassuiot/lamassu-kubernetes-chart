apiVersion: v1
kind: Service
metadata:
  name: ocsp
  namespace: {{.Release.Namespace}}
spec:
  selector:
    app: ocsp
  type: ClusterIP
  ports:
  - name: http
    port: 8085
    targetPort: 8085
    protocol: TCP