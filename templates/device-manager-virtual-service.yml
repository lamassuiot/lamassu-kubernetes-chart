apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: device-manager
  namespace: {{.Release.Namespace}}
spec:
  hosts:
  - "{{.Values.domain}}"
  gateways:
  - default-gateway
  http:
  - match:
    - uri:
        prefix: /api/devmanager/
    rewrite:
      uri: /
    route:
    - destination:
        port:
          number: 8085
        host: device-manager