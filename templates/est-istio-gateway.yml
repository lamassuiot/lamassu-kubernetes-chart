apiVersion: networking.istio.io/v1alpha3
kind: Gateway
metadata:
  name: est-gateway
  namespace: {{.Release.Namespace}}
spec:
  selector:
    istio: ingressgateway # use Istio default gateway implementation
  servers:
  - port:
      number: 443
      name: https
      protocol: HTTPS
    tls:
      mode: MUTUAL
    hosts:
      - lamassu.zpd.ikerlan.es
